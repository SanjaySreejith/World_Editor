public enum EditMode {
    // If you wish to add more Edit Modes, add them here. They must be given a case in keyPressed in VirtualWorld
    HOUSE, OBSTACLE, TREE, GRASS, FLOWERS, BRIDGE, DIRT, DEFAULT, REMOVE
}
